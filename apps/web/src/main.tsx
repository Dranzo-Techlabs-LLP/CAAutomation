import React from 'react';
import ReactDOM from 'react-dom/client';

function App() {
  return <div>CA Practice Management dashboard scaffold</div>;
}

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
);
